# Random-Maze-Path-Generator
A C program that generates a 9x9 maze grid initialized with walls. It implements a 'Random Walk' algorithm to carve a guaranteed traversable path from point A (0,0) to point B (8,8) and visualizes the result.
